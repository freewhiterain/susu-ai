[project]
name = "xiaosu-mock-api"
version = "0.1.0"
description = "小苏内部 API Mock 服务"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.32.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
